{% extends 'admin/adminbase.html.twig' %}
{% block title %}Daily Shop | Alt Kategoriler{% endblock %}
{% block brand %}Alt Kategoriler{% endblock %}
{% block body %}
    {% block m6 %}<li class="active">{% endblock %}

    <div class="table-responsive ">
        <table class="table table-hover table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>GenderNo</th>
                <td>{{ category.GenderNo }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category.Name }}</td>
            </tr>
            <tr>
                <th>CreatedDate</th>
                <td>{{ category.CreatedDate ? category.CreatedDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_category_index') }}">listeye dön</a>

    <a href="{{ path('admin_category_edit', {'id': category.id}) }}">düzenle</a>

    {{ include('admin/category/_delete_form.html.twig') }}
    </div>
{% endblock %}
